{
  "name": "livepeer-modules-transcode-gateway",
  "version": "0.1.0",
  "description": "Livepeer Video Gateway — ABR + RTMP live transcode gateway backed by the Livepeer network",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "engines": {
    "node": ">=24",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.0.0"
}
